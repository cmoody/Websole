(function($){
	
	var commands = [
		{ name: "hud_show 0"},
		{ name: "hud_show 1"}
	];
	
})(JQuery);


// pressing up shows history and typing autocompletes from command list
// Store history in array and check the length to make sure its not more that X